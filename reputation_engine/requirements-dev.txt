# Dev/test tooling. CI installs this; runtime deps live in requirements.txt.
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100
ruff>=0.6
mypy>=1.10
bandit>=1.7
pre-commit>=3.7
